from settings import config

ALIASES = {
    1: (config.STREAMER_NICKNAME, config.SC2_PLAYER_ACCOUNTS),
    2: ("BARCODE", config.SC2_BARCODE_ACCOUNTS),
    3: ("Computer AI", config.SC2_COMPUTER_ACCOUNTS)
}